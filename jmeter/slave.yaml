apiVersion: apps/v1
kind: Deployment
metadata:
  name: jmeter-slaves
  namespace: performance
  labels:
    jmeter_mode: slave
spec:
  replicas: 2
  selector:
    matchLabels:
      jmeter_mode: slave
  template:
    metadata:
      labels:
        jmeter_mode: slave
    spec:
      securityContext:
        runAsUser: 0
      containers:
      - name: jmslave
        image: youketero/jmeter:k8s
        imagePullPolicy: Always
        env:
        - name: MODE
          value: "SLAVE"
        command: ["/bin/bash"]
        args: ["-c", "trap 'exit 0' SIGUSR1 && until [ -f /opt/jmeter/load_test ]; do sleep 5; done && /bin/bash /opt/jmeter/load_test"]
        ports:
        - containerPort: 1099
        - containerPort: 50000
        - containerPort: 4445
        resources:
          requests:
            memory: "1024Mi"
            cpu: "2048m"
          limits:
            memory: "1024Mi"
            cpu: "2048m"
        livenessProbe:
          exec:
            command: ["pgrep", "-f", "jmeter"]
          initialDelaySeconds: 10
          periodSeconds: 10
